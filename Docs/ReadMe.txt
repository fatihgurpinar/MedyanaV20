

1) if you want to run ths project firstly create tables with example data.
   You can use respectively; 
     
     -CreateDB.txt
     -CreateClinicTable.txt 
     -CreateEquipmentTable.txt 
     -CreateMedyanaUser.txt under the DBScripts folder.

2) Run CoreApiService Api project in one of the ways below

   2.1 run dotnet run in the root folder of  CoreApiService
   2.2 open project in VS 2019 press ctrl+f5

   In both way, you should see all API methods definitions in swagger doc at http://localhost:65427/swagger
